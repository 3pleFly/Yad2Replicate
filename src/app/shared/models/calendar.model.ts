export interface CalendarDate {
  date: Date;
  selected?: boolean;

  // constructor(date: Date, selected?: boolean) {
  //   this.date = date;
  //   if (selected) this.selected = selected;
  // }
}
